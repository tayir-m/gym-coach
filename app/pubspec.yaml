name: gym_coach
description: 健身猫头鹰 - Duolingo 式健身 & 饮食计划 app
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: '>=3.32.0'

dependencies:
  flutter:
    sdk: flutter

  drift: ^2.30.0
  drift_flutter: ^0.2.8
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.1.0
  path: ^1.9.0

  freezed_annotation: ^2.4.0
  json_annotation: ^4.9.0

  lottie: ^3.1.0

  go_router: ^14.0.0

  flutter_riverpod: ^2.5.0

  http: ^1.2.0
  crypto: ^3.0.3

  flutter_local_notifications: ^17.0.0
  timezone: ^0.9.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  drift_dev: ^2.30.0
  build_runner: ^2.4.0

  freezed: ^3.2.0
  json_serializable: ^6.10.0
